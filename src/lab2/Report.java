package lab2;

import java.time.LocalDate;

public class Report
{
int id;
LocalDate reportDate;
int workCompleted;
int workRemaining;
static int workCompleted(Sprint s)
{
	//extract all the feature list of given list (ll)
	// process the list based on ll.isComplete attribute
	// to calculate workComplete: count no of features in list which has isComplete set
return 0;
}
static int remainingWork(Sprint s)
{
	//extract all the feature list of given list (ll)
	// process the list based on ll.isComplete attribute
	// to calculate workComplete: count no of features in list which has isComplete is not set
	return 0;
}
}
